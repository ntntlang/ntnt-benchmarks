production:
  adapter: postgresql
  database: benchmarks
  username: ntnt
  password: <%= ENV.fetch('DB_PASSWORD', 'changeme') %>
  host: <%= ENV.fetch('DB_HOST', 'localhost') %>
  port: 5432
  pool: 25
  prepared_statements: true
